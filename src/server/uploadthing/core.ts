
/**
 * File router for the application
 * learn more about uploadthing here: @see https://docs.uploadthing.com/getting-started/appdir
 */



// Stub uploadthing core to avoid importing uploadthing and env during static builds.
// Exports a minimal `ourFileRouter` and a type placeholder for `OurFileRouter` so
// client components can still import types without causing build-time side effects.
export type OurFileRouter = Record<string, unknown>;
export const ourFileRouter: OurFileRouter = {};
